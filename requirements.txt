# ─────────────────────────────────────────────────────────────────
#  Bumblebee Wake Word – requirements
#  Python 3.11 ONLY  (3.12 breaks several deps)
#
#  Install ORDER matters – follow README.md instructions exactly.
# ─────────────────────────────────────────────────────────────────

# Core ML
torch==2.2.2
torchaudio==2.2.2
numpy>=1.24.0,<2.0.0

# openWakeWord  (install AFTER torch so it can't downgrade it)
openwakeword>=0.6.0

# TTS – works on Windows natively
piper-tts>=1.2.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0
audiomentations>=0.33.0

# Data / misc
datasets>=2.14.0,<5.0.0
tqdm>=4.66.0
scikit-learn>=1.3.0
pyyaml>=6.0.0
requests>=2.31.0

# Model export
onnx>=1.15.0
onnxruntime>=1.16.0

# Live microphone test (easier on Windows than pyaudio)
sounddevice>=0.4.6
